export const metadata = {
  title: 'Blog | Lucas Fernández',
  description: 'Artículos y notas sobre desarrollo web.',
}

const BlogLayout = ({ children }) => {
  return <>{children}</>
}

export default BlogLayout
